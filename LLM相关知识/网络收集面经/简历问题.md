# Compkey
1. 进程和线程的区别？
进程是具有一定独立功能的程序关于某个数据集合上的一次运行活动,进程是系统进行资源分配和调度的一个独立单位.

线程是进程的一个实体,是CPU调度和分派的基本单位,它是比进程更小的能独立运行的基本单位.线程自己基本上不拥有系统资源,只拥有一点在运行中必不可少的资源(如程序计数器,一组寄存器和栈),但是它可与同属一个进程的其他的线程共享进程所拥有的全部资源.


2.关系
一个线程可以创建和撤销另一个线程;同一个进程中的多个线程之间可以并发执行.

相对进程而言，线程是一个更加接近于执行体的概念，它可以与同进程中的其他线程共享数据，但拥有自己的栈空间，拥有独立的执行序列。


3.区别
进程和线程的主要差别在于它们是不同的操作系统资源管理方式。进程有独立的地址空间，一个进程崩溃后，在保护模式下不会对其它进程产生影响，而线程只是一个进程中的不同执行路径。线程有自己的堆栈和局部变量，但线程之间没有单独的地址空间，一个线程死掉就等于整个进程死掉，所以多进程的程序要比多线程的程序健壮，但在进程切换时，耗费资源较大，效率要差一些。但对于一些要求同时进行并且又要共享某些变量的并发操作，只能用线程，不能用进程。

1) 简而言之,一个程序至少有一个进程,一个进程至少有一个线程.

2) 线程的划分尺度小于进程，使得多线程程序的并发性高。

3) 另外，进程在执行过程中拥有独立的内存单元，而多个线程共享内存，从而极大地提高了程序的运行效率。

4) 线程在执行过程中与进程还是有区别的。每个独立的线程有一个程序运行的入口、顺序执行序列和程序的出口。但是线程不能够独立执行，必须依存在应用程序中，由应用程序提供多个线程执行控制。

5) 从逻辑角度来看，多线程的意义在于一个应用程序中，有多个执行部分可以同时执行。但操作系统并没有将多个线程看做多个独立的应用，来实现进程的调度和管理以及资源分配。这就是进程和线程的重要区别。

4.优缺点
线程和进程在使用上各有优缺点：线程执行开销小，但不利于资源的管理和保护；而进程正相反。同时，线程适合于在SMP机器上运行，而进程则可以跨机器迁移。



2. 简单描述一下项目

厂商投放广告时不仅要考虑自己的营销策略，还要考虑自己企业在行业内的竞争地位，识别竞争性关键词可以有效调整自己的营销策略。我们根据Comp公式计算每个种子关键词及其竞争性关键词的竞争度。

数据来源 ： 搜狗搜素数据，我们选取了10个出现频次最高且无歧义的关键词作为种子关键词。

数据清洗阶段： 我们使用NIO以块的方式切割文件，对切割后的若干文件开多线程同时读取，使用Hanlp分词器中的NLP算法进行分词，将助词、副词和量词等无关词性的词剔除，使用TF-IDF算法屏蔽一些比较广泛的词汇如图片。

算法流程： 先使用分词效果好的分词器对词条进行分词f0。
	筛选出包含种子关键字s的词条文件f1。
	统计f1中的词频生成文件f2。
	筛选f1词频中前十五个词作为中介关键词。
	统计包含中介关键词的词条文件f3。
	统计f3中的词频（ 排除中介关键词和种子关键词）生成文件f4。
	取f4中前十五个作为竞争关键词。
	根据Compkey算法计算竞争度。

前端： 使用Vue等框架，
后端: 使用SpringBoot框架，在持久化层利用redis的缓存机制实现高并发访问，同时结合数据库主从复制的机制实现数据库读写分离。
部署：一个三台服务器，一台配置nginx实现反向代理与负载均衡，两台服务器部署项目后端连接redis与Mysql数据库

数据库设计： 向“主服务器”里写数据，从“从服务器”里读数据的。先到“从Redis服务器”里读取缓存，如果找不到，就再到从mysql数据库里去读。如果从“从mysql数据库”里读到数据，那么需要写入“主Redis”,而根据Redis集群的主从复制机制，该数据会被写入“从Redis服务器”。



3. 什么是NIO？和普通IO有什么区别？
传统IO是1个连接1个线程，阻塞式，面向流。逐个字节读取。
NIO是1个线程管理多个连接，非阻塞式，面向缓冲区。

NIO核心是通道和缓冲区，把文件分块放到缓冲区，然后直接读下一个分块，不需要等。

4. Hanlp分词器中的NLP算法的原理是什么？

5. 什么是反向代理？
以代理服务器来接受Internet上的连接请求，然后将请求转发给内部网络上的服务器，并将从服务器上得到的结果返回给internet上请求连接的客户端。反向代理可以防止主服务器被恶意攻击，为负载均衡和动静分离提供实现支持，提高访问速度，当经过首次链接之后，代理服务器会将此次数据缓存下来，当下次用户来访问时，可以优先在缓存中查找，提高了访问速度，可以在代理服务器上设置规则用来过滤一些不良信息。

负载均衡是指使用反向代理同时代理多个相同内容的应用服务器，将客户端请求分发到各个应用服务器上并接收响应返回给客户端。当一台服务器的单位时间内的访问量越大时，服务器压力就越大，大到超过自身承受能力时，服务器就会崩溃。为了避免服务器崩溃，让用户有更好的体验，我们通过负载均衡的方式来分担服务器压力，保证服务器集群中的每个服务器压力趋于平衡，分担服务器的压力，避免服务器崩溃的情况。

6. redis为什么快？缓存机制怎么实现

Redis本质上是一个Key-Value类型的内存数据库，整个数据库加载在内存当中操作，读写性能很高。


7. 数据库的ACID特性？

原子性：事务是不可分割的操作单元
一致性：事务执行前后，数据库从一个一致状态转变为另一个一致状态
隔离性： 并发事务互不干扰
持久性：事务一旦提交，其更改永久保存，即使系统崩溃也不会丢失数据


8. 数据库的外键？

9. 数据库主从复制怎么实现？
可以直接修改数据库配置完成。原理如下：
主库记录二进制日志： 当主库执行完提交事务后，会将所有可能引起数据变更的SQL语句，记录到二进制日志中。
从库I/O线程拉取日志： 从库会启动一个I/O线程，它会连接到主库，主库上有一个特殊的Binlog Dump线程，负责根据从库的请求，将二进制日志中的事件推送给它。I/O线程接收到这些事件后，会将其写入到从库本地的中继日志
从库SQL线程重放事件：从库中的SQL线程会读取中继日志中的事件，并按照事件发生的先后顺序在从库上重新执行（重放）这些SQL语句，从而使得从库的数据与主库保持一致

# 在线教育
1. 简单介绍项目
有三个角色，老师，学生和管理员，每个角色设计的模块和功能不同。
前端： 我们使用Vue框架搭建而成。
后端： 分成数据库层、服务层和控制层，MVC架构，和数据层交互采用Mybatis-plus框架

2. 什么是MVC架构？
MVC 架构程序的工作流程： （1）用户通过 View 页面向服务端提出请求，可以是表单请求、超链接请求、AJAX 请求等 （2）服务端 Controller 控制器接收到请求后对请求进行解析，找到相应的 Service对用户请求进行处理 （3）Service处理后，将处理结果再交给 Controller （4）Controller 在接到处理结果后，根据处理结果找到要作为向客户端发回的响应 View 页面。页面经渲染（数据填充）后，再发送给客户端。

3. TCP三次握手
客户端发送一个SYN（同步）标志位设置为1的数据包，其中包含一个随机初始序列号（如seq=J），表明请求建立连接，客户端随之进入SYN-SENT状态。服务器收到后，回复一个SYN和ACK（确认）标志位均设置为1的数据包，其中的确认号ack=J+1是对客户端序列号的确认，同时服务器也发出自己的随机序列号（如seq=K），并进入SYN-RCVD状态。最后，客户端检查确认号无误后，再次发送一个ACK标志位为1的数据包，确认号ack=K+1，服务器验证通过后，双方即进入ESTABLISHED状态，连接建立成功，可以开始数据传输


4. SPringBoot约定优于配置是什么意思？
框架提供一系列合理的默认配置和约定，开发者只有在需要偏离这些约定时才进行显式配置。这样能大幅减少决策和配置工作量，提升开发效率。

# reference

1. https://topjavaer.cn/java/java-basic.html


